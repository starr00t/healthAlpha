# OpenAI API 설정 (서버 전용 - 보안)
# https://platform.openai.com/api-keys 에서 발급받은 API 키를 입력하세요
# 주의: NEXT_PUBLIC_ 접두사를 사용하지 마세요! (클라이언트 노출 방지)
OPENAI_API_KEY=your-api-key-here

# 사용 예시:
# OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 중요 보안 사항:
# ✅ OPENAI_API_KEY (서버 전용) - 안전함
# ❌ NEXT_PUBLIC_OPENAI_API_KEY - 위험! 클라이언트에 노출됨

# 주의사항:
# 1. API 키는 절대 공개 저장소에 커밋하지 마세요
# 2. .gitignore에 .env.local이 포함되어 있는지 확인하세요
# 3. OpenAI API는 유료입니다 (사용량에 따라 과금)
# 4. gpt-4o-mini 모델 권장 (저렴하고 빠름)
# 5. gpt-3.5-turbo는 더 저렴한 옵션

# 서버 배포 시:
# - Vercel: 환경 변수에 OPENAI_API_KEY 추가
# - Netlify: 환경 변수에 OPENAI_API_KEY 추가
# - 다른 플랫폼: 각 플랫폼의 환경 변수 설정 방법 참고

# 비용 참고 (2024년 1월 기준):
# - gpt-3.5-turbo: $0.0005 / 1K tokens
# - gpt-4o-mini: $0.00015 / 1K tokens (입력), $0.0006 / 1K tokens (출력)
# - gpt-4o: $0.005 / 1K tokens (입력), $0.015 / 1K tokens (출력)

# 한 번의 건강 조언 요청 = 약 500-1000 tokens
# 월 100회 사용 시 예상 비용: gpt-4o-mini 기준 약 $0.05-0.10

# Rate Limiting 권장:
# - 사용자당 하루 10-20회 제한
# - IP당 시간당 5-10회 제한
# - 비용 초과 방지를 위한 모니터링 설정
